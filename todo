# TODO

## Done

- The slider UI and mechanishm has to be remade for the new UI. done
- remove the icon from Search all. done
- match slider to the side?. done (not needed)
- tab change reloads songs. player search is not affected. done
- If beatmap not available then put a grey/ muted pink color cross X on the checkmark.
  If beatmap is has a artist match flag then have a small red exclamation mark next to
  the checkbox. done
- on adding playlists after the first one (at the bottom of the queue) have a pop up
  notification similar to the download one. done
- No matching beatmapset found — the artist could not be verified has to be worded
  clearer. done
- Pulling Ranked and LOved re fetches the list. Fix that. done
- can slider refetch be stopped since we already have everything?. done (resolved with
  a button)

## Remaining

- select all confirmed matches (come up with a good name) button instead of Select all since the tickmark of the top row does the same thing.
- mode switcher has yt,spotify, AM, single song combine them into playlist/song search. So then we will have song/playlist saerch and player search
- is there memory leak problem? the cached beatmaps and pics have to be deleted when the user pulls up a new search or player search (is song preview cached?)
- song preview should show a loading or errors. (network) as song as its loading it should show a buffering animation
- breakcore playlist has 11 songs but only 7 are fetched. This is the playlist https://music.youtube.com/playlist?list=PLJU2iuLr5pzzwD1BjsQSX4Exb-w1Wc2Kp.
- are we loading every image and beatmap for the player search at once? is it expensive?
  if yes then we could have dynamic scroll based loading... if loading everything at once consuming less api calls, then so be it. (Don change this one without confirmation from user)
- the player search menu cards(eg - most played) dont expand to full length. we could have a fullscreen expand menu with the title eg Most played" header on top of the screen docked like the
  search bar. The animation should be fluid and similar to the searhbar. Once one card
  ends the next card docks to the top like Functions in an IDE like VSCode.
- maybe a little bounce on every hover animation? toggleable
- YouTube playlists stop at 100 songs. Raise the cap to 500: follow the Innertube
  continuation token (one paced request per extra 100), keep `truncated` structural,
  and show the popup only past 500. In the same change, pace Search All under the
  60/min /api/osu/search limit so a 500 song Search All does not end in "osu! is busy"
  rows. Overrides the F-18 "keep 100" decision in REBUILD_PLAN.md.
- The top bar that has the download options is not global. When doing player search it deletes all the old song search results that we ticked and vice versa.
- the Change player button and the trash icon is confusing. Change player deletes the search and so does the trash icon. I think change player should just go back to the prev screen with the player results.
- need to redo F-46 with a better approach. (confirm approach with user)
- Re:Re: split

## Long term goals

- eventually expand to enable creation of playlist from osu collections or lists or
  beatmaps. LT Goal
- Need a full remake of the code, its too specific and small goal focused. A full rebuild
  would be more consistent. LT Goal
